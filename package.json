{
  "name": "mini-delta-force",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/domain/expedition.js && node scripts/check-assets.mjs"
  },
  "engines": { "node": ">=22" }
}
